openapi: 3.1.0
info:
  title: Api
  version: 0.6.0
  description: Mo AI Voice Assistant API
servers:
  - url: /api
    description: Base API path
tags:
  - name: health
    description: Health operations
  - name: mo
    description: Mo AI voice assistant operations
paths:
  /healthz:
    get:
      operationId: healthCheck
      tags: [health]
      summary: Health check
      responses:
        "200":
          description: Healthy
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/HealthStatus"
  /mo/chat:
    post:
      operationId: moChat
      tags: [mo]
      summary: Send a message to Mo
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/ChatRequest"
      responses:
        "200":
          description: Mo's response
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ChatResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /mo/speak:
    post:
      operationId: moSpeak
      tags: [mo]
      summary: Convert text to speech via ElevenLabs
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/SpeakRequest"
      responses:
        "200":
          description: Audio stream (mpeg)
          content:
            audio/mpeg:
              schema:
                type: string
                format: binary
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /mo/voice:
    post:
      operationId: moVoice
      tags: [mo]
      summary: Full voice pipeline for native mobile
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/VoiceRequest"
      responses:
        "200":
          description: Transcript, reply text, and base64 audio
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/VoiceResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
  /mo/transcribe-live:
    post:
      operationId: moTranscribeLive
      tags: [mo]
      summary: Transcribe a partial in-progress recording for live captions
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: "#/components/schemas/TranscribeLiveRequest"
      responses:
        "200":
          description: Partial transcript text (may be empty)
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TranscribeLiveResponse"
        "500":
          description: Server error
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/ErrorResponse"
components:
  schemas:
    HealthStatus:
      type: object
      properties:
        status:
          type: string
      required: [status]
    ConversationMessage:
      type: object
      properties:
        role:
          type: string
          enum: [user, assistant]
        content:
          type: string
      required: [role, content]
    UserPreferences:
      type: object
      properties:
        name:
          type: string
        location:
          type: string
        timezone:
          type: string
        responseLength:
          type: string
          enum: [short, medium, long]
          default: medium
    MemoryItem:
      type: object
      properties:
        id:
          type: string
        category:
          type: string
          enum: [personal, preferences, schedule, goals]
        key:
          type: string
        value:
          type: string
        createdAt:
          type: number
        updatedAt:
          type: number
      required: [id, category, key, value, createdAt, updatedAt]
    MemoryAction:
      type: object
      properties:
        action:
          type: string
          enum: [save, delete]
        category:
          type: string
          enum: [personal, preferences, schedule, goals]
        key:
          type: string
        value:
          type: string
      required: [action, key]
    Task:
      type: object
      properties:
        id:
          type: string
        title:
          type: string
        dueDate:
          type: string
        status:
          type: string
          enum: [pending, completed]
        category:
          type: string
        createdAt:
          type: number
        updatedAt:
          type: number
        completedAt:
          type: number
      required: [id, title, status, createdAt, updatedAt]
    TaskAction:
      type: object
      properties:
        action:
          type: string
          enum: [add, complete, delete]
        title:
          type: string
        dueDate:
          type: string
        category:
          type: string
      required: [action, title]
    ReminderContext:
      type: object
      description: Upcoming reminder sent as context with each request
      properties:
        id:
          type: string
        title:
          type: string
        content:
          type: string
        datetime:
          type: string
      required: [id, title, content, datetime]
    ReminderData:
      type: object
      description: New reminder created by Mo (returned in response)
      properties:
        title:
          type: string
        content:
          type: string
        datetime:
          type: string
      required: [title, content, datetime]
    ReminderAction:
      type: object
      description: Action taken on an existing reminder (returned in response)
      properties:
        action:
          type: string
          enum: [delete, dismiss]
        title:
          type: string
          description: Title keyword to match the reminder to act on
      required: [action, title]
    NoteContext:
      type: object
      description: Recent note for context
      properties:
        id:
          type: string
        content:
          type: string
        title:
          type: string
        category:
          type: string
        timestamp:
          type: number
      required: [id, content, timestamp]
    NoteData:
      type: object
      description: New note created by Mo
      properties:
        content:
          type: string
        title:
          type: string
          description: Short title extracted from note content (3–6 words)
        category:
          type: string
          description: "Optional category: idea | meeting | personal | work | other"
        timestamp:
          type: string
      required: [content]
    NoteAction:
      type: object
      description: Action taken on an existing note
      properties:
        action:
          type: string
          enum: [delete]
        keyword:
          type: string
          description: Keyword from the note content or title to identify and remove it
      required: [action, keyword]
    ChatRequest:
      type: object
      properties:
        message:
          type: string
        mode:
          type: string
          enum: [executive, daily, luxury]
          default: executive
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ConversationMessage"
        preferences:
          $ref: "#/components/schemas/UserPreferences"
        memories:
          type: array
          items:
            $ref: "#/components/schemas/MemoryItem"
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/Task"
        reminders:
          type: array
          items:
            $ref: "#/components/schemas/ReminderContext"
          description: Upcoming reminders for context
        notes:
          type: array
          items:
            $ref: "#/components/schemas/NoteContext"
          description: Recent notes for context
      required: [message]
    ChatResponse:
      type: object
      properties:
        reply:
          type: string
        functionCalled:
          type: string
        reminder:
          $ref: "#/components/schemas/ReminderData"
        reminderAction:
          $ref: "#/components/schemas/ReminderAction"
        note:
          $ref: "#/components/schemas/NoteData"
        memoryAction:
          $ref: "#/components/schemas/MemoryAction"
        noteAction:
          $ref: "#/components/schemas/NoteAction"
        taskAction:
          $ref: "#/components/schemas/TaskAction"
      required: [reply]
    TranscribeLiveRequest:
      type: object
      properties:
        audio:
          type: string
          description: Base64-encoded partial audio (streamable container, e.g. ADTS AAC)
        format:
          type: string
          enum: [m4a, mp4, wav, caf, aac, webm]
          default: aac
      required: [audio]
    TranscribeLiveResponse:
      type: object
      properties:
        text:
          type: string
      required: [text]
    SpeakRequest:
      type: object
      properties:
        text:
          type: string
      required: [text]
    VoiceRequest:
      type: object
      properties:
        audio:
          type: string
          description: Base64-encoded audio file (ADTS AAC on iOS and Android, WebM on web)
        format:
          type: string
          enum: [m4a, mp4, wav, caf, aac, webm]
          default: aac
        mode:
          type: string
          enum: [executive, daily, luxury]
          default: executive
        messages:
          type: array
          items:
            $ref: "#/components/schemas/ConversationMessage"
        preferences:
          $ref: "#/components/schemas/UserPreferences"
        memories:
          type: array
          items:
            $ref: "#/components/schemas/MemoryItem"
        tasks:
          type: array
          items:
            $ref: "#/components/schemas/Task"
        reminders:
          type: array
          items:
            $ref: "#/components/schemas/ReminderContext"
          description: Upcoming reminders for context
        notes:
          type: array
          items:
            $ref: "#/components/schemas/NoteContext"
          description: Recent notes for context
      required: [audio]
    VoiceResponse:
      type: object
      properties:
        transcript:
          type: string
        reply:
          type: string
        audioBase64:
          type: string
        functionCalled:
          type: string
        reminder:
          $ref: "#/components/schemas/ReminderData"
        reminderAction:
          $ref: "#/components/schemas/ReminderAction"
        note:
          $ref: "#/components/schemas/NoteData"
        noteAction:
          $ref: "#/components/schemas/NoteAction"
        memoryAction:
          $ref: "#/components/schemas/MemoryAction"
        taskAction:
          $ref: "#/components/schemas/TaskAction"
      required: [transcript, reply, audioBase64]
    ErrorResponse:
      type: object
      properties:
        error:
          type: string
      required: [error]
